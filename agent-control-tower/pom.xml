<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.3.5</version>
        <relativePath/>
    </parent>

    <groupId>io.aria.conductor</groupId>
    <artifactId>agent-control-tower</artifactId>
    <version>0.1.0-SNAPSHOT</version>
    <packaging>pom</packaging>
    <name>Aria Conductor</name>

    <modules>
        <module>act-common</module>
        <module>act-agent</module>
        <module>act-execution</module>
        <module>act-knowledge</module>
        <module>act-aria</module>
        <module>act-dashboard-api</module>
        <module>act-app</module>
        <module>act-test-support</module>
    </modules>

    <properties>
        <java.version>21</java.version>
        <maven.compiler.source>21</maven.compiler.source>
        <maven.compiler.target>21</maven.compiler.target>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <springdoc.version>2.6.0</springdoc.version>
        <caffeine.version>3.1.8</caffeine.version>
        <logstash-logback.version>7.4</logstash-logback.version>
        <archunit.version>1.3.0</archunit.version>
        <jqwik.version>1.9.1</jqwik.version>
        <pitest.version>1.16.1</pitest.version>
        <pitest-junit5.version>1.2.1</pitest-junit5.version>
        <!-- JaCoCo ratchet: per-module overrides raise these; never set above measured coverage -->
        <jacoco.line.minimum>0</jacoco.line.minimum>
        <jacoco.branch.minimum>0</jacoco.branch.minimum>
        <!-- CI lane switches: unit lane sets skip.integration.tests, integration lane sets skip.unit.tests.
             Both default to ${skipTests} so plain `mvn install -DskipTests` still skips everything
             (explicit <skipTests> in plugin config would otherwise override the CLI flag). -->
        <skipTests>false</skipTests>
        <skip.unit.tests>${skipTests}</skip.unit.tests>
        <skip.integration.tests>${skipTests}</skip.integration.tests>
    </properties>

    <dependencyManagement>
        <dependencies>
            <!-- Internal modules -->
            <dependency>
                <groupId>io.aria.conductor</groupId>
                <artifactId>act-common</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>io.aria.conductor</groupId>
                <artifactId>act-agent</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>io.aria.conductor</groupId>
                <artifactId>act-execution</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>io.aria.conductor</groupId>
                <artifactId>act-knowledge</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>io.aria.conductor</groupId>
                <artifactId>act-aria</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>io.aria.conductor</groupId>
                <artifactId>act-dashboard-api</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>io.aria.conductor</groupId>
                <artifactId>act-test-support</artifactId>
                <version>${project.version}</version>
            </dependency>

            <!-- SpringDoc OpenAPI -->
            <dependency>
                <groupId>org.springdoc</groupId>
                <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
                <version>${springdoc.version}</version>
            </dependency>

            <!-- Caffeine -->
            <dependency>
                <groupId>com.github.ben-manes.caffeine</groupId>
                <artifactId>caffeine</artifactId>
                <version>${caffeine.version}</version>
            </dependency>

            <!-- Structured JSON logging -->
            <dependency>
                <groupId>net.logstash.logback</groupId>
                <artifactId>logstash-logback-encoder</artifactId>
                <version>${logstash-logback.version}</version>
            </dependency>

            <!-- ArchUnit: architecture guardrail tests -->
            <dependency>
                <groupId>com.tngtech.archunit</groupId>
                <artifactId>archunit-junit5</artifactId>
                <version>${archunit.version}</version>
            </dependency>

            <!-- jqwik: property-based tests for parsers / round-trips / state machines -->
            <dependency>
                <groupId>net.jqwik</groupId>
                <artifactId>jqwik</artifactId>
                <version>${jqwik.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <!-- Lombok globally available -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>
    </dependencies>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-maven-plugin</artifactId>
                    <configuration>
                        <excludes>
                            <exclude>
                                <groupId>org.projectlombok</groupId>
                                <artifactId>lombok</artifactId>
                            </exclude>
                        </excludes>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <configuration>
                        <source>21</source>
                        <target>21</target>
                        <compilerArgs>
                            <arg>--enable-preview</arg>
                        </compilerArgs>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
            </plugin>
            <!--
              Test tiering:
              - Surefire (mvn test): fast unit tier only. *IntegrationTest / *E2ETest /
                *ContractTest are excluded and run under Failsafe instead.
              - Failsafe (mvn verify): integration tier, sequential (Spring context reuse
                matters more than parallelism there).
              CI unit lane runs `mvn test`; integration lane runs `mvn verify`.
            -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <skipTests>${skip.unit.tests}</skipTests>
                    <excludes>
                        <exclude>**/*IntegrationTest.java</exclude>
                        <exclude>**/*E2ETest.java</exclude>
                        <exclude>**/*ContractTest.java</exclude>
                    </excludes>
                    <!-- forkCount=1: several existing tests assert real wall-clock timing with
                         tight thresholds (CircuitBreaker 30ms iteration latency, AdkHttpClient 3s
                         health probe). Parallel forks cause CPU contention that flakes them.
                         Parallelism comes from the CI module-group matrix (4 runners), not local
                         forks — keep this at 1 for deterministic timing tests. -->
                    <forkCount>1</forkCount>
                    <reuseForks>true</reuseForks>
                    <argLine>@{argLine} --enable-preview -XX:+EnableDynamicAgentLoading</argLine>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-failsafe-plugin</artifactId>
                <configuration>
                    <skipTests>${skip.integration.tests}</skipTests>
                    <includes>
                        <include>**/*IntegrationTest.java</include>
                        <include>**/*E2ETest.java</include>
                    </includes>
                    <argLine>@{argLine} --enable-preview -XX:+EnableDynamicAgentLoading</argLine>
                </configuration>
                <executions>
                    <execution>
                        <goals>
                            <goal>integration-test</goal>
                            <goal>verify</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <version>0.8.12</version>
                <executions>
                    <execution>
                        <id>prepare-agent</id>
                        <goals>
                            <goal>prepare-agent</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>report</id>
                        <phase>test</phase>
                        <goals>
                            <goal>report</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>check</id>
                        <!-- Bind to test phase so the unit lane (`mvn test`) enforces the ratchet.
                             Skip follows the unit tier switch: integration lane and -DskipTests
                             builds must not evaluate coverage from absent/stale exec data.
                             Filtered local runs (-Dtest=Foo) under-cover by design: add -Djacoco.skip=true. -->
                        <phase>test</phase>
                        <goals>
                            <goal>check</goal>
                        </goals>
                        <configuration>
                            <skip>${skip.unit.tests}</skip>
                            <rules>
                                <rule>
                                    <element>BUNDLE</element>
                                    <limits>
                                        <limit>
                                            <counter>LINE</counter>
                                            <value>COVEREDRATIO</value>
                                            <minimum>${jacoco.line.minimum}</minimum>
                                        </limit>
                                        <limit>
                                            <counter>BRANCH</counter>
                                            <value>COVEREDRATIO</value>
                                            <minimum>${jacoco.branch.minimum}</minimum>
                                        </limit>
                                    </limits>
                                </rule>
                            </rules>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <repositories>
        <repository>
            <id>central</id>
            <url>https://repo.maven.apache.org/maven2</url>
        </repository>
    </repositories>

    <profiles>
        <!--
          Mutation testing (nightly lane only — too slow for PR CI).
          Usage: mvn -P mutation org.pitest:pitest-maven:mutationCoverage -pl act-execution,act-knowledge
          Report-only for the first two weeks; ratchet mutationThreshold once scores stabilise.
        -->
        <profile>
            <id>mutation</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.pitest</groupId>
                        <artifactId>pitest-maven</artifactId>
                        <version>${pitest.version}</version>
                        <dependencies>
                            <dependency>
                                <groupId>org.pitest</groupId>
                                <artifactId>pitest-junit5-plugin</artifactId>
                                <version>${pitest-junit5.version}</version>
                            </dependency>
                        </dependencies>
                        <configuration>
                            <targetClasses>
                                <param>io.aria.conductor.*</param>
                            </targetClasses>
                            <targetTests>
                                <param>io.aria.conductor.*</param>
                            </targetTests>
                            <excludedTestClasses>
                                <param>*IntegrationTest</param>
                                <param>*E2ETest</param>
                            </excludedTestClasses>
                            <withHistory>true</withHistory>
                            <timestampedReports>false</timestampedReports>
                            <mutationThreshold>0</mutationThreshold>
                            <outputFormats>
                                <outputFormat>XML</outputFormat>
                                <outputFormat>HTML</outputFormat>
                            </outputFormats>
                            <!-- Single-threaded + capped heap: the default auto-threading forks
                                 one JVM per core and each fork boots a full Spring context,
                                 which crashed the runner JVM on every nightly run (hs_err).
                                 timeoutConstant=15000: the 4s default is below the wall-clock
                                 budget of the Spring-backed tests, so healthy mutation units
                                 were killed as TIMED_OUT and rerun repeatedly. -->
                            <threads>1</threads>
                            <timeoutConstant>15000</timeoutConstant>
                            <jvmArgs>
                                <jvmArg>--enable-preview</jvmArg>
                                <jvmArg>-Xmx2048m</jvmArg>
                                <jvmArg>-XX:MaxMetaspaceSize=768m</jvmArg>
                            </jvmArgs>
                        </configuration>
                    </plugin>
                    <!-- PIT inherits the surefire argLine, which contains the @{argLine}
                         late-binding placeholder that PIT cannot resolve — the raw string
                         reached the minion JVM and killed it at startup (MINION_DIED,
                         "could not open `{argLine}'"). Override both test plugins here so
                         PIT's minions get a clean argLine (no JaCoCo agent: PIT runs its
                         own coverage instrumentation). -->
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-surefire-plugin</artifactId>
                        <configuration>
                            <argLine>--enable-preview -XX:+EnableDynamicAgentLoading</argLine>
                        </configuration>
                    </plugin>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-failsafe-plugin</artifactId>
                        <configuration>
                            <argLine>--enable-preview -XX:+EnableDynamicAgentLoading</argLine>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
    <pluginRepositories>
        <pluginRepository>
            <id>central</id>
            <url>https://repo.maven.apache.org/maven2</url>
        </pluginRepository>
    </pluginRepositories>
</project>


